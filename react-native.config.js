/**
 * React Native asset config — registers bundled fonts for Android/iOS linking.
 * Run `npx react-native-asset` after adding files under assets/fonts.
 */
module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./assets/fonts/'],
};
