<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/faceRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Passive unlock — hidden preview satisfies CameraX on devices that reject analysis-only binding. -->
    <androidx.camera.view.PreviewView
        android:id="@+id/facePreview"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:layout_gravity="top|start"
        android:alpha="0"
        android:visibility="invisible" />

    <!-- Dimmed scrim — mimics the OS biometric sheet without showing the camera feed. -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#99000000" />

    <LinearLayout
        android:id="@+id/faceBottomPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@android:color/white"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingHorizontal="28dp"
        android:paddingTop="32dp"
        android:paddingBottom="28dp">

        <ImageView
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/face_lock_icon_bg"
            android:contentDescription="@null"
            android:padding="12dp"
            android:src="@drawable/ic_face_lock" />

        <TextView
            android:id="@+id/faceTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:gravity="center"
            android:text="Unlock Karins"
            android:textColor="#111111"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/faceSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:text="Look at your device"
            android:textColor="#666666"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/faceCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:paddingHorizontal="16dp"
            android:paddingVertical="10dp"
            android:text="Cancel"
            android:textColor="#1976D2"
            android:textSize="15sp"
            android:textStyle="bold" />
    </LinearLayout>
</FrameLayout>
